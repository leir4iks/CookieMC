--- a/net/minecraft/world/entity/projectile/FireworkRocketEntity.java
+++ b/net/minecraft/world/entity/projectile/FireworkRocketEntity.java
@@ -1,5 +_,6 @@
 package net.minecraft.world.entity.projectile;
 
+import io.cookiemc.cookie.Config;
 import it.unimi.dsi.fastutil.doubles.DoubleDoubleImmutablePair;
 import java.util.List;
 import java.util.OptionalInt;
@@ -137,6 +_,11 @@
                     }
                 });
             }
+            // cookie start - Threaded Regions
+            if (this.attachedToEntity != null && !io.cookiemc.cookie.region.ServerRegions.isTickThreadFor(this.attachedToEntity)) {
+                this.attachedToEntity = null;
+            }
+            // cookie end
 
             if (this.attachedToEntity != null) {
                 Vec3 handHoldingItemAngle;
@@ -354,4 +_,14 @@
         double d1 = entity.position().z - this.position().z;
         return DoubleDoubleImmutablePair.of(d, d1);
     }
+    // cookie start
+
+    @Override
+    public boolean shouldBeSaved() {
+        if (Config.INSTANCE.entities.disableFireworkSaving) {
+            return false;
+        }
+        return super.shouldBeSaved();
+    }
+    // cookie end
 }
