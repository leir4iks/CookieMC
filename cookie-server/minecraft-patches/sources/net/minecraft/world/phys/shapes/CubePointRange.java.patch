--- a/net/minecraft/world/phys/shapes/CubePointRange.java
+++ b/net/minecraft/world/phys/shapes/CubePointRange.java
@@ -3,23 +_,25 @@
 import it.unimi.dsi.fastutil.doubles.AbstractDoubleList;
 
 public class CubePointRange extends AbstractDoubleList {
-    private final int parts;
+    private final int size; // cookie - replace parts by size
+    private final double scale; // cookie - replace division by multiplication
 
     public CubePointRange(int parts) {
         if (parts <= 0) {
             throw new IllegalArgumentException("Need at least 1 part");
         } else {
-            this.parts = parts;
+            this.size = parts + 1; // cookie - replace parts by size
         }
+        this.scale = 1.0D / parts; // cookie - replace division by multiplication
     }
 
     @Override
     public double getDouble(int value) {
-        return (double)value / this.parts;
+        return value * this.scale; // cookie - replace division by multiplication
     }
 
     @Override
     public int size() {
-        return this.parts + 1;
+        return this.size; // cookie - replace parts by size
     }
 }
