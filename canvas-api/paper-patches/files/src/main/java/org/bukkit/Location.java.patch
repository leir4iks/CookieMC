--- a/src/main/java/org/bukkit/Location.java
+++ b/src/main/java/org/bukkit/Location.java
@@ -29,12 +29,15 @@
  * representation by the implementation.
  */
 public class Location implements Cloneable, ConfigurationSerializable, io.papermc.paper.math.FinePosition {
-    private java.lang.ref.Reference<org.bukkit.World> world;
-    private double x;
-    private double y;
-    private double z;
-    private float pitch;
-    private float yaw;
+    private volatile java.lang.ref.Reference<org.bukkit.World> world;
+    // Canvas start - volatile
+    private volatile double x;
+    private volatile double y;
+    private volatile double z;
+    private volatile float pitch;
+    private volatile float yaw;
+    // Canvas end
 
     /**
      * Constructs a new Location with the given coordinates
